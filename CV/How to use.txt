First open the terminal and write "npm i". That command will install all of the needed dependencies.

Then write "npm run start" which will run the lite-server and the program will be opened in a web browser.